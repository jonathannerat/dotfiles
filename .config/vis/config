# visualizer refresh rate
visualizer.fps=30

# output defined in mpd's config gile
mpd.fifo.path=/tmp/mpd.fifo

# ellipse and lorenz look wrong in my st setup
visualizers=spectrum

# prevent wiz from f'ing up my PS1
colors.override.terminal=false

# custom teal theme
colors.scheme=teal
