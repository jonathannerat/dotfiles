# vi: fdm=marker
# === MEDIA === {{{
XF86MonBrightness{Up,Down}
	light -{A,U} 10

XF86Audio{Lower,Raise}Volume
	pamixercap -{d,i} 5

shift + XF86Audio{Lower,Raise}Volume
	pamixer -{d,i} 5

XF86AudioMute
	pamixer -t

XF86Audio{Play,Prev,Next}
	autoplayerctl {play-pause,previous,next}

ctrl + shift + XF86AudioPrev
	autoplayerctl position 0

ctrl + XF86Audio{Prev,Next}
	autoplayerctl position 10{-,+}

shift + XF86Audio{Prev,Next}
	autoplayerctl position 60{-,+}
#}}}

# === APPS === {{{
super + shift + p
	rofi -show drun

super + shift + Return
	alacritty

super + ctrl + Return
	alacritty --class=floating-alacritty

super + ctrl + e
	alacritty --class=floating-alacritty -e n

super + alt + {u,i,o,p}
	{browser,zattab,xournalpp,pavucontrol}
#}}}

# === OTHER === {{{
# Helper dmenus
super + shift + {ctrl + u, u, o}
	dmenu-{unicode -C,unicode,kaomojis} -z 700 -l 20 -fn "Noto Sans:style=Regular:pixelsize=16:autohint=true:antialias=true"

super + shift + i
	dmenu-nficons -z 500 -l 20 -fn "TerminessTTF Nerd Font:style=Regular:pixelsize=16:autohint=true:antialias=true"

super + shift + b
	passmenu -z 500 -l 20

# Screenshots
{,super,shift,ctrl,shift+ctrl} + @Print
	imgkprnt -{c,cm,cw,caO,ca}

# View a screenshot
super + @s
    image="$(mktemp --suffix=.png)" ; \
    imgkprnt -a -o "$image" ; \
    size="$(identify -format '%wx%h' "$image")" ; \
    sxiv -g "$size" -b "$image" && rm "$image"
#}}}
